# 产品需求

本目录保存评审通过后形成的最终 PRD。`draft` 和 `review` 成果可以暂时没有 PRD；成果进入 `approved` 或 `archived` 前必须有一个同名 PRD，例如：

```text
deliverables/progress-management/
product/requirements/progress-management.md
```

最终 PRD 至少包含：背景、目标用户、业务目标、范围、非目标、核心场景、业务规则和验收标准，并与对应原型或 Demo 的确认版本保持一致。
